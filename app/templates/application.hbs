<container>
  <vbox>
    <vbox>
      {{nav-bar isAuthenticated=session.isAuthenticated logout=(action "invalidateSession")}}
    </vbox>
    <vbox class="main">{{outlet}}</vbox>
  </vbox>
</container>
